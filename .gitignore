# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# Django
*.log
*.sqlite3
db.sqlite3
staticfiles/
media/
backend/competition_backups/

# Node / React
node_modules/
build/
dist/
.npm/
*.tsbuildinfo

# Environment / secrets
.env
.env.local
.env.*.local
*.pem
*.key

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Docs binaries (keep in sync with specs/)
docs/*.docx
docs/*.pdf

# Local agent state
.claude/

# RAG source PDFs (large binaries / third-party content) — keep catalog manifest, not the files
docs/rag_sources/staging/

# aide-checks execution receipt: written per run, proof of a run rather than
# a fact about the tree. CI uploads it as an artifact instead.
checks/.last-run.json
