# Pinned to what the production backend runs (checked 2026-09-15). This file
# had not changed since the fork from globalstrat and still named Django 4.2 and
# psycopg 3 while the service ran Django 5.2 on psycopg2; a server rebuilt from
# it would have installed a framework older than the migrations it must run.
Django==5.2.4
asgiref==3.9.1
sqlparse==0.5.3
djangorestframework==3.16.1
django-cors-headers==4.7.0
django-filter==25.1
psycopg2-binary==2.9.10
PyJWT==2.12.1
PyYAML==5.4.1
python-dotenv==1.1.1
tzdata==2025.2
typing_extensions==4.14.1
httpx==0.28.1
requests==2.32.4
dashscope==1.25.14
qdrant-client==1.17.0
sentence-transformers==5.0.0
pypdf==6.9.1
python-docx==1.2.0

# Test-only: lets `manage.py test --parallel` report a worker's traceback.
# Without it one unpicklable exception aborts the worker and every test queued
# on it is reported as failed.
tblib==3.2.2

# The systemd unit runs the distribution's /usr/bin/gunicorn (Ubuntu package,
# 20.1.0), not a pip install.
