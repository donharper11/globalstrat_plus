{
  "stack": "mixed",
  "repo": "globalstrat+",
  "notes": "Django backend + React frontend, mid-upgrade (CRV2-10 line). A live agent worktree exists at .claude/worktrees/agent-aa23a70e43fabef2c and is untracked; this check tests trackedness, so it is unaffected and was not touched.",
  "checks": {
    "no-committed-secrets": {
      "enabled": true,
      "mode": "blocking",
      "fast": true,
      "include": ["docs/**", "README.md"],
      "exclude": [],
      "exclusion_reasons": {},
      "mode_reason": "Promoted to blocking 2026-09 once the four findings recorded in SECURITY-NOTICE.md §3 were dispositioned. All four were read and confirmed non-credentials; see allow_fields_reasons and allow_findings.",
      "allow_fields": ["DJANGO_SECRET_KEY"],
      "allow_fields_reasons": {
        "DJANGO_SECRET_KEY": "Fixture value 'crv208-walkthrough' in the CRV2 audit harnesses under handoff_readiness_v2/evidence/post-close-disputes/harness/ (start_stack.py:28, disputes.py:25). Named for the walkthrough it drives, used only by the disposable evidence stack, never a production value. Read and confirmed 2026-09."
      },
      "allow_findings": {
        "rework/browser_pass.js:jwt:12": "Expired instructor walkthrough token. Payload decoded 2026-09: role instructor, exp 1783947018 (April 2026), an 8-hour token dead five months. Browser-pass test credential, not a live secret.",
        "rework/browser_pass.js:jwt:13": "Expired student walkthrough token from the same browser pass. Same provenance and expiry window as line 12."
      }
    },
    "no-agent-worktrees-tracked": {
      "enabled": true,
      "mode": "blocking",
      "fast": true,
      "patterns": [".claude/worktrees", "worktrees"],
      "patterns_source": "This repo's .gitignore line 45 ignores all of .claude/, which is where its harness places worktrees — there is a live one at .claude/worktrees/agent-aa23a70e43fabef2c right now. Bare .claude is not used as a pattern: the check tests trackedness, and the narrower path is the one the convention names."
    }
  }
}
