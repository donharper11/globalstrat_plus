# Credentials for the disposable postgres container, and machine state.
# Pids, ports and server logs are machine state, not evidence; they do not
# belong in the repository, and the database password never does.
dbenv
.pgenv
.pgpw
runtime/
